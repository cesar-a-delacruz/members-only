{
  "name": "express-postgresql-template",
  "private": true,
  "version": "0.0.0",
  "main": "app.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "npx prettier . --write",
    "app": "node --watch app.js",
    "schema": "psql -d members_only -f ./db/schema.sql"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
